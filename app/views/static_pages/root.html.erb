<script>
  <% if logged_in? %>
    window.currentUser = <%=
      render(
        "api/users/user.json.jbuilder",
        user: current_user
      ).html_safe
    %>
  <% end %>

  window.staticImages = {
    profile_pic_url: "<%= asset_url('profile_pic.svg') %>"
  }
</script>

<div id="root">
</div>